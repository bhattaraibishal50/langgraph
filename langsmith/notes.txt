Why Langsmith ??
AI job application assistant
-> Reading the job description (JD) from a link or PDF
-> Seaerching the user's portfolio, resume and past workflow
-> Matfhing the most releveant skills to the jiob
-> Generate a tailored cover letter
-> Profferading the final letter for grammer and tone 


Latency 2 minutes : overse in particular days the mail has occured it is slow 7-10 minutes
Debug the system and it is complex llm workflow and it has mulitple stage. 
In this tools like langsmith come to practices


Research assistant -> Agent 
Recieve a query -> eg: "summarize the top 5 latest pappers on solar energy storage"
Search academic sources for relevant papers -> google scholors, 
Read each paper and extract key poitns
Generate a detailed summary with citation
Asnser follow up questions if th user aks

Cost problem token usages -> Langsmith is required. 

RAG senior: 
TCS -> organization -> lakh -> 1000s as freshers
Rules around leave policy, notice period, health insurance. 
Fresher dont understand and ask
All documents of company -> use llm and chatbot. 
* user asks a questions
* REtertuive find relevant docuiments frm the knowledge based
* LLM reads the documents and generates an answer
* User gets a netural , cionversation response. 

Halligunating: retriervel haligunate, generator (LLM) low quality local llm
Lanmgsmith. 


Observability to understand the system internal state by examining its external output like logs metrics and treces
It allows you to dignoise issues, understad performacne and improve realiabity by analyzing data generated by the system.

LLm -> not deterministic 
Input -> output might be different 
Langsmith is unified observaklity & evaluate platform wher eteams can debug test and mionitor AI app performance
What input is take and what output and also time, everything is recorded 


What does langsmith trace ?
1. I/O 
2. All the intermediate steps
3. Latency (both at appliation level and component level)
4. Token usages
5. Cost
6. Error 
7. Tags
8. Metadata
9. Feedback


Core concept
Project: query | prompt | llm | Parser
Trace : single execuation is called trace
Run : execuation withn the component, we have 3 run (promot, llm, paraser)

In RAG ??
User query
Retrived documents
LLM prompts
LLM response

Default:
* by default langsmith trace not chains
traceable decorators to trace the rag components.

If code is added first time means first-even change -> build index
PDF ciontent changes, even if medata data
If chunking params changes -> chunk zie or chunk overlap
Embeeding models name changes -> eg from text-embeeding models. 
It will take some time. 
In production app we must only if 


Langgraph
* Graph executation can be logged in Langsmith as a trace
* Each node (eg: retriver, LLM, tool call, subgraph) becomes as a run inside the trace. 
* Yu can visulize the path taken.
* START -> Retiever -> reranker -> LLm answer -> end. 
if a workflow branches (conftional/parelle/subgraph)_ Langsmithch captchers wich path was executed. 

